{
  "name": "<%= _.slugify(appname) %>",
  "version": "0.0.1",<% if (enableTests) { %>
  "scripts": {
    "test": "./node_modules/grunt-cli/bin/grunt test"
  },<% } %>
  "main": "src/index.js",
  "devDependencies": {
    "grunt": "~0.4.0",
    "grunt-contrib-jshint": "~0.6.0",<% if (enableBrowserSupport) { %>
    "grunt-browserify": "~1.2.0",<% } if (enableTests) { %>
    "grunt-mocha-cli": "~1.3.0",
    "mocha": "~1.14.0",
    "chai": "~1.8.1",
    "sinon": "~1.7.3",<% } %>
    "bower": "~0.9.2",
    "load-grunt-tasks": "~0.2.0"
  }
}
